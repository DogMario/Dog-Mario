using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticClearedStages : MonoBehaviour {

    public static bool[] cleared = { false, false, false, false, false };  // keep track of whether a stage has been cleared
    //  after all stages cleared reset all to false
    //  1 based indexing
}
