using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisableInputAtStart : MonoBehaviour {

    //idk how do this yet

    /*Animator anim;

    void Start() {
        anim = GetComponent<Animator>();
    }
    

    IEnumerator RunThisShit(bool canmove) {
        while (true) {
            if (canmove)
                Time.timeScale = 1;
            else
                Time.timeScale = 0;
        }
    }*/
}
